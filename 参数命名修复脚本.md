# 前后端参数命名不匹配修复指南

## 问题说明
前端使用 camelCase 格式，后端期望 snake_case 格式，导致参数对应不上。

## 需要修复的参数映射

### 通用字段
- `studentId` → `student_id`
- `semesterId` → `semester_id` 
- `courseId` → `course_id`
- `classId` → `class_id`
- `classCourseId` → `class_course_id`
- `pricePerHour` → `price_per_hour`
- `enrolledAt` → `enrolled_at`
- `startDate` → `start_date`
- `endDate` → `end_date`
- `startTime` → `start_time`
- `endTime` → `end_time`
- `checkedAt` → `checked_at`

## 修复进度

### ✅ 已修复
- **班级管理页面** (`ClassManagement.vue`)
  - 模板字段名: `semesterId` → `semester_id`
  - 模板字段名: `courseId` → `course_id`  
  - 模板字段名: `pricePerHour` → `price_per_hour`
  - formData 对象属性
  - 验证规则字段名
  - API 调用参数名

- **课表管理页面** (`ScheduleManagement.vue`)  
  - 模板字段名: `classCourseId` → `class_course_id`
  - formData 对象属性
  - 验证规则字段名

### 🔄 需要修复的页面

#### 报名管理页面 (`EnrollmentManagement.vue`)
- `studentId` → `student_id`
- `classCourseId` → `class_course_id`
- `enrolledAt` → `enrolled_at`

#### 考勤管理页面 (`AttendanceManagement.vue`)  
- `studentId` → `student_id`
- `checkedAt` → `checked_at`

#### 账单管理页面 (`BillingManagement.vue`)
- `studentId` → `student_id`
- `semesterId` → `semester_id`

## 修复步骤 (适用于每个页面)

1. **修改模板中的字段名**
   ```vue
   <!-- 修改前 -->
   <a-form-item name="studentId">
   <a-input v-model:value="formData.studentId" />
   
   <!-- 修改后 -->
   <a-form-item name="student_id">
   <a-input v-model:value="formData.student_id" />
   ```

2. **修改 formData 对象属性**
   ```javascript
   // 修改前
   const formData = reactive({
     studentId: undefined,
     classCourseId: undefined
   });
   
   // 修改后
   const formData = reactive({
     student_id: undefined,
     class_course_id: undefined
   });
   ```

3. **修改验证规则**
   ```javascript
   // 修改前
   const rules = {
     studentId: [{ required: true, message: '请选择学生' }]
   };
   
   // 修改后  
   const rules = {
     student_id: [{ required: true, message: '请选择学生' }]
   };
   ```

4. **修改赋值和重置逻辑**
   ```javascript
   // 修改前
   formData.studentId = record.studentId;
   formData.studentId = undefined;
   
   // 修改后
   formData.student_id = record.student_id;
   formData.student_id = undefined;
   ```

5. **检查API调用中的参数名**
   确保发送给后端的数据使用 snake_case 格式

## 验证方法
1. 检查浏览器开发者工具的 Network 面板
2. 确认请求 payload 中的字段名是 snake_case 格式
3. 测试创建、编辑、删除功能是否正常 