# 教育培训机构管理系统 - 前端使用指南

## 1. 启动前端应用

### 步骤1：进入前端目录
```bash
cd demo_fe
```

### 步骤2：安装依赖（如果还没有安装）
```bash
npm install
```

### 步骤3：启动开发服务器
```bash
npm run dev
```

### 步骤4：访问应用
启动成功后，在浏览器中打开：
```
http://localhost:5173
```

## 2. 登录系统

### 管理员登录
1. 点击页面上的"管理员登录"链接
2. 使用管理员账号登录：
   - 用户名：`admin`
   - 密码：`admin123`（或您设置的管理员密码）

## 3. 测试新增功能

### 快速测试页面
1. 登录后，在左侧菜单中点击**"功能测试"**
2. 这个页面可以快速测试所有管理功能是否正常工作
3. 点击**"一键测试所有功能"**按钮来验证系统状态

### 学生管理测试
1. 在左侧菜单点击**"学生管理"**
2. 点击右上角的**"添加学生"**按钮
3. 应该会弹出一个对话框让您输入学生信息：
   - 学生姓名
   - 联系方式
   - 优惠率
4. 填写信息后点击"确定"

### 班级管理测试
1. 在左侧菜单点击**"班级管理"**
2. 点击右上角的**"添加班级"**按钮
3. 应该会弹出对话框让您输入：
   - 班级名称
   - 所属学期

### 课程管理测试
1. 在左侧菜单点击**"课程管理"**
2. 点击右上角的**"添加课程"**按钮
3. 应该会弹出对话框让您输入课程名称

## 4. 故障排除

### 如果点击"添加"按钮没有反应：

1. **检查浏览器控制台**：
   - 按 F12 打开开发者工具
   - 查看 Console 标签是否有错误信息
   - 应该能看到 "showCreateModal 被调用" 的日志

2. **检查网络连接**：
   - 确保后端服务已启动（通常在 :8080 端口）
   - 在浏览器中访问 `http://localhost:8080/api/h1/student` 测试后端是否正常

3. **清除浏览器缓存**：
   - 按 Ctrl+F5 强制刷新页面
   - 或在开发者工具中右键刷新按钮选择"清空缓存并强制刷新"

4. **检查登录状态**：
   - 确保您已正确登录管理员账号
   - 如果没有登录，API 请求会被拒绝

### 如果遇到其他问题：

1. **重启前端开发服务器**：
   ```bash
   # 在demo_fe目录下
   npm run dev
   ```

2. **重启后端服务器**：
   ```bash
   # 在demo目录下
   go run main.go
   ```

3. **查看控制台日志**：
   - 前端控制台（浏览器F12）
   - 后端控制台（终端输出）

## 5. 验证功能是否正常

### 成功的标志：
1. 点击"添加学生"按钮后，会弹出一个包含表单的对话框
2. 表单包含"学生姓名"、"联系方式"、"优惠率"字段
3. 填写并提交后，学生会被添加到列表中
4. 浏览器控制台显示成功的API调用日志

### 调试信息：
- 点击"添加学生"按钮时，浏览器控制台应该显示：
  ```
  showCreateModal 被调用
  modalVisible 当前值: false
  modalVisible 设置后的值: true
  ```
- 页面右上角应该显示"正在打开创建学生对话框..."的提示

## 6. 其他管理功能

系统还包含以下管理功能：
- **学期管理**：创建和管理学期
- **班级管理**：创建班级并分配到学期
- **课程管理**：管理课程信息
- **报名管理**：学生报名课程
- **课表管理**：安排课程时间
- **打卡管理**：学生签到和请假
- **账单统计**：自动生成学费账单

所有这些功能都有对应的"添加"按钮，操作方式类似。

---

如果按照以上步骤操作后仍然无法正常使用新增功能，请提供：
1. 浏览器控制台的错误信息
2. 后端服务器的日志输出
3. 具体在哪一步遇到了问题 