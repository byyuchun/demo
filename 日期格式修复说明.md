# 前端日期格式修复说明

## 问题描述
前端发送给后端的日期格式为 `YYYY-MM-DD`，但后端Go程序需要RFC3339格式（ISO 8601格式），导致日期解析失败。

## 修复内容

### 1. 创建日期工具函数 (`demo_fe/src/utils/dateUtils.ts`)
- `toRFC3339(date, isEndOfDay)` - 将日期转换为RFC3339格式
- `datetimeToRFC3339(datetime)` - 将日期时间转换为RFC3339格式  
- `formatDateDisplay(date)` - 格式化日期用于UI显示
- `formatDatetimeDisplay(datetime)` - 格式化日期时间用于UI显示

### 2. 修复的页面和字段

#### 学期管理 (`SemesterManagement.vue`)
- **开始日期 (start_date)**: `YYYY-MM-DD` → RFC3339 (当天开始时间)
- **结束日期 (end_date)**: `YYYY-MM-DD` → RFC3339 (当天结束时间)

#### 课表管理 (`ScheduleManagement.vue`)  
- **课程日期 (date)**: `YYYY-MM-DD` → RFC3339 (当天开始时间)

#### 报名管理 (`EnrollmentManagement.vue`)
- **报名日期 (enrolledAt)**: `YYYY-MM-DD` → RFC3339 (当天开始时间)

#### 考勤管理 (`AttendanceManagement.vue`)
- **查询开始日期 (startDate)**: `YYYY-MM-DD` → RFC3339 (当天开始时间)
- **查询结束日期 (endDate)**: `YYYY-MM-DD` → RFC3339 (当天结束时间)  
- **补签时间 (checkedAt)**: `YYYY-MM-DD HH:mm:ss` → RFC3339 (保持原有时间)

#### 账单管理 (`BillingManagement.vue`)
- **显示日期格式化**: 使用新的工具函数

### 3. 格式转换示例

#### 修改前
```javascript
// 学期开始/结束日期
start_date: formData.startDate.format('YYYY-MM-DD')  // "2024-01-01"
end_date: formData.endDate.format('YYYY-MM-DD')      // "2024-06-30"

// 课程日期
date: formData.date.format('YYYY-MM-DD')             // "2024-01-15"

// 补签时间
checkedAt: formData.checkedAt.format('YYYY-MM-DD HH:mm:ss')  // "2024-01-15 14:30:00"
```

#### 修改后  
```javascript
// 学期开始/结束日期 (使用工具函数)
start_date: toRFC3339(formData.startDate, false)     // "2024-01-01T00:00:00.000Z"
end_date: toRFC3339(formData.endDate, true)          // "2024-06-30T23:59:59.999Z"

// 课程日期 (使用工具函数)
date: toRFC3339(formData.date, false)                // "2024-01-15T00:00:00.000Z"

// 补签时间 (使用工具函数)
checkedAt: datetimeToRFC3339(formData.checkedAt)     // "2024-01-15T14:30:00.000Z"
```

### 4. 时区处理说明
- **开始日期**: 设为当天00:00:00 UTC时间
- **结束日期**: 设为当天23:59:59 UTC时间  
- **日期时间**: 保持用户选择的具体时间，转换为UTC时间

### 5. 显示格式保持不变
UI显示的日期格式保持原样：
- 日期显示: `YYYY-MM-DD` (如: 2024-01-01)
- 日期时间显示: `YYYY-MM-DD HH:mm:ss` (如: 2024-01-01 14:30:00)

## 测试建议
1. 创建新学期，检查开始和结束日期是否正常保存
2. 添加课程安排，检查日期是否正确
3. 学生报名功能测试
4. 考勤查询和补签功能测试
5. 账单生成和查询功能测试

## 注意事项
- 所有传给后端的日期现在都是UTC时间的RFC3339格式
- 前端显示仍然使用本地时间格式
- 新的日期工具函数统一了项目中的日期处理逻辑
- 修复后端可以正确解析所有日期字段 