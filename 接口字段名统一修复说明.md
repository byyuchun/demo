# 前后端接口字段名统一修复说明

## 问题描述

前端和后端之间存在字段命名不统一的问题：
- **前端发送数据**：原本使用 `camelCase`，现已修复为 `snake_case`
- **后端返回数据**：使用 `snake_case` 格式
- **前端接口定义**：原本使用 `camelCase`，导致TypeScript类型检查失败

## 解决方案

统一使用 `snake_case` 格式，确保前后端数据格式完全一致。

## 修复内容总结

### 1. 班级管理页面 (`ClassManagement.vue`)

#### 接口定义修复
```typescript
// 修复前
interface Class {
  semesterId: number;
}

interface ClassCourse {
  courseId: number;
  pricePerHour: number;
}

// 修复后
interface Class {
  semester_id: number;
}

interface ClassCourse {
  course_id: number;
  price_per_hour: number;
}
```

### 2. 课表管理页面 (`ScheduleManagement.vue`)

#### 接口定义修复
```typescript
// 修复前
interface Schedule {
  classCourseId: number;
}

// 修复后
interface Schedule {
  class_course_id: number;
}
```

### 3. 报名管理页面 (`EnrollmentManagement.vue`)

#### 接口定义修复
```typescript
// 修复前
interface Enrollment {
  studentId: number;
  classCourseId: number;
  enrolledAt: string;
}

// 修复后
interface Enrollment {
  student_id: number;
  class_course_id: number;
  enrolled_at: string;
}
```

### 4. 考勤管理页面 (`AttendanceManagement.vue`)

#### 接口定义修复
```typescript
// 修复前
interface Attendance {
  studentId: number;
  scheduleId: number;
  checkedAt: string;
  makeupScheduleId: number;
}

// 修复后
interface Attendance {
  student_id: number;
  schedule_id: number;
  checked_at: string;
  makeup_schedule_id: number;
}
```

#### 表单数据修复
```typescript
// 修复前
const makeupFormData = reactive({
  studentId: undefined,
  scheduleId: undefined,
  makeupScheduleId: undefined,
  checkedAt: null,
});

// 修复后
const makeupFormData = reactive({
  student_id: undefined,
  schedule_id: undefined,
  makeup_schedule_id: undefined,
  checked_at: null,
});
```

#### 模板字段名修复
```vue
<!-- 修复前 -->
<a-form-item name="studentId">
<a-input v-model:value="makeupFormData.studentId" />

<!-- 修复后 -->
<a-form-item name="student_id">
<a-input v-model:value="makeupFormData.student_id" />
```

#### 验证规则修复
```typescript
// 修复前
const makeupRules = {
  studentId: [{ required: true, message: '请选择学生' }],
  scheduleId: [{ required: true, message: '请选择课程安排' }],
};

// 修复后
const makeupRules = {
  student_id: [{ required: true, message: '请选择学生' }],
  schedule_id: [{ required: true, message: '请选择课程安排' }],
};
```

## 字段名映射表

| 前端原字段名(camelCase) | 统一后字段名(snake_case) | 说明 |
|------------------------|-------------------------|------|
| `studentId` | `student_id` | 学生ID |
| `semesterId` | `semester_id` | 学期ID |
| `courseId` | `course_id` | 课程ID |
| `classId` | `class_id` | 班级ID |
| `classCourseId` | `class_course_id` | 教学班ID |
| `pricePerHour` | `price_per_hour` | 每小时价格 |
| `enrolledAt` | `enrolled_at` | 报名时间 |
| `startDate` | `start_date` | 开始日期 |
| `endDate` | `end_date` | 结束日期 |
| `startTime` | `start_time` | 开始时间 |
| `endTime` | `end_time` | 结束时间 |
| `checkedAt` | `checked_at` | 签到时间 |
| `scheduleId` | `schedule_id` | 课表ID |
| `makeupScheduleId` | `makeup_schedule_id` | 补课课表ID |

## 修复后的数据流

### 前端发送数据 → 后端
```json
{
  "name": "测试班级",
  "semester_id": 1,
  "course_id": 2,
  "price_per_hour": 100.00
}
```

### 后端返回数据 → 前端
```json
{
  "id": 1,
  "name": "测试班级",
  "semester_id": 1,
  "course_id": 2,
  "price_per_hour": 100.00,
  "created_at": "2024-01-01T00:00:00Z"
}
```

### 前端TypeScript接口
```typescript
interface ClassCourse {
  id: number;
  name: string;
  semester_id: number;
  course_id: number;
  price_per_hour: number;
  created_at: string;
}
```

## 验证方法

1. **编译检查**：确保TypeScript编译无错误
2. **运行时测试**：
   - 创建新记录
   - 编辑现有记录
   - 查询数据列表
   - 检查数据是否正确显示和保存

3. **网络面板检查**：
   - 按F12打开开发者工具
   - 查看Network面板中的请求/响应数据格式
   - 确认所有字段名都是snake_case格式

## 注意事项

- ✅ 所有前端接口定义已统一为snake_case
- ✅ 所有表单数据结构已统一为snake_case  
- ✅ 所有模板字段名已统一为snake_case
- ✅ 所有验证规则已统一为snake_case
- ✅ 所有API调用参数已统一为snake_case
- ✅ 所有数据访问代码已统一为snake_case

修复完成后，前后端数据交互将完全统一，不再有字段名不匹配的问题。 